name: Deploy Documentation

on:
  push:
    branches:
      - main
      - master
    paths:
      - USER_GUIDE.md
      - documentation/**
      - mkdocs.yml
      - requirements-docs.txt
      - scripts/assemble_user_guide.py
      - scripts/sync_docs_version.py
      - src/__init__.py
      - .github/workflows/deploy-docs.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install docs dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-docs.txt

      # Stamps the version and the date of the commit being deployed. Done here
      # rather than at commit time so the published page always reflects HEAD;
      # the working-tree edit is never committed back.
      - name: Stamp version and commit date
        run: python scripts/sync_docs_version.py

      - name: Build MkDocs site
        run: mkdocs build --clean

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
